# legacy/

이 폴더는 대체된 구버전 구현을 참고용으로만 보관한다. 실제로 서비스되지 않는다.

- `goal.html` — 원래는 "내 목표" 페이지를 자체 인라인 Firebase 로직으로 구현한
  독립 페이지였다. 지금은 `docs/index.html`의 "내 목표" 탭 + `docs/goal-app.js`로
  대체됐다.

현재 어디에서도 이 폴더를 링크하지 않는다. `docs/` 바깥에 있는 이유도 GitHub Pages가
이 폴더를 서빙하지 않도록 하기 위함이다.
